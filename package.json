{
  "name": "mini-api",
  "version": "1.0.0",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "jest",
    "lint": "npx eslint src",
    "api-test": "newman run postman/suma_collection.json",
    "start:test": "concurrently --kill-others-on-fail --success first \"node src/app.js\" \"npx newman run postman/suma_collection.json\""
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "eslint": "^8.57.1",
    "jest": "^29.0.0",
    "newman": "^6.2.1",
    "supertest": "^6.3.3"
  }
}
